'use client';

import styled from "styled-components";

const TabDiv = styled.div`

`;

const Tabs = (): JSX.Element => {

    return (
        <TabDiv>
            Tabs
        </TabDiv>
    )
}

export default Tabs;